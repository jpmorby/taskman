<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Seeded Admin Password
    |--------------------------------------------------------------------------
    |
    | Password given to the admin account created by the database seeder. Leave
    | this empty and the seeder will generate a random password and print it
    | once. The seeder never runs the admin block in production.
    |
    */

    'admin_password' => env('SEED_ADMIN_PASSWORD'),

];
